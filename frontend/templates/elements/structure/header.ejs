<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <a class="navbar-brand" href="/">NEAT</a>
        </div>
        <div class="navbar-collapse collapse">
            <% if(!locals.user) { %>
            <form class="navbar-form navbar-right" ng-controller="authLogin" ng-submit="login()">
                <div class="form-group" ng-class="{'has-error': error}">
                    <input type="text" placeholder="Username/Email" ng-model="data.username" class="form-control">
                </div>
                <div class="form-group" ng-class="{'has-error': error}">
                    <input type="password" ng-model="data.password" placeholder="Password" class="form-control">
                </div>
                <button type="submit" class="btn btn-success">Sign in</button>
            </form>
            <% } else { %>
            <ul class="nav navbar-nav navbar-right">
                <li><a href="/admin/">Admin</a></li>
                <li><a href="/logout">Logout</a></li>
            </ul>
            <% } %>
        </div>
    </div>
</nav>